<h1>All Topics</h1>

<%= link_to 'Add New Topic', new_admin_topic_path %>
<br><br>

<% @topics.each do |topic| %>
  <p><%=link_to topic.name, edit_admin_topic_path(topic) %> <%= link_to 'Delete', admin_topic_path(topic), method: :delete %></p>
<% end %>
