<h1>Edit Your Info</h1>

<%= form_for(current_user) do |f|%>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :bio %>
  <%= f.text_area :bio %>

  <%= f.submit 'Submit Changes' %>
<% end %>
